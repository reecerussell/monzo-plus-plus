syntax = "proto3";

package proto;

message SendRequest {
    string UserID = 1;
    string JSONData = 2;
}

message EmptySendResponse {
}

service PluginService {
    rpc Send(SendRequest) returns (EmptySendResponse) {}
}